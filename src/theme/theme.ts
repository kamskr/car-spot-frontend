import { createTheme, ThemeOptions } from '@mui/material';

export const theme: ThemeOptions = createTheme({
  palette: {
    primary: {
      main: '#7d44f5',
    },
    secondary: {
      main: '#F64A90',
    },
    background: {
      paper: '#fff',
      default: '#F1F1F1',
    },
  },
});

export interface Routes {
  home: string;
  login: string;
  register: string;
  profile: string;
  myCars: string;
}
export const routes: Routes = {
  home: '/',
  login: '/login',
  register: '/register',
  profile: '/profile',
  myCars: '/my-cars',
};

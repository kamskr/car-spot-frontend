export interface Routes {
  home: string;
  login: string;
  register: string;
  profile: string;
}
export const routes: Routes = {
  home: '/',
  login: '/login',
  register: '/register',
  profile: '/profile',
};
